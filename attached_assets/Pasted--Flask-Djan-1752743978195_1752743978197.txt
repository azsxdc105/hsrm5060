صياغة البرومبت: انسخ النص كاملاً وضعه في أداة توليد الكود. سيقوم النموذج بإنشاء مشروع (Flask أو Django) منظم بمجلدات، مع تعليمات تشغيل، قاعدة بيانات، قوالب واجهة عربية/إنجليزية، وأدوات إرسال إيميل آمن.

🔷 القسم 0: دور النموذج المطلوب

أنت مهندس برمجيات كبير (Senior Software Engineer) ومتخصص في بناء أنظمة ويب آمنة عالية الاعتمادية في Python (Flask أو Django) مع خبرة في البريد الإلكتروني، التعامل مع الملفات، الأمن، قواعد البيانات، والخدمات السحابية. تعرف معايير بناء أنظمة مؤسسة (Enterprise) وقابلية التوسع.

🔷 القسم 1: نظرة عامة على النظام

نظام ويب مركزي يسمح لموظفي شركة (أو وسطاء مطالبات) بإرسال مطالبات مالية إلى شركات التأمين في السعودية بشكل منظم وموحّد. يدعم:

إدخال بيانات المطالبة يدويًا أو استخراجها تلقائيًا من ملف (PDF/صورة) باستخدام ذكاء اصطناعي (OCR + NLP).

رفع ملفات متعددة (صور حادث، تقرير نجم، هوية، تقدير ورشة، فواتير، تقرير طبية...).

اختيار شركة التأمين من قائمة شاملة (مع بريد/بريدات المطالبات لكل شركة).

توليد رسالة إيميل رسمية قابلة للتخصيص حسب الشركة.

إرسال تلقائي عبر SMTP (SSL/TLS) مع سجلات.

لوحة تحكم لإدارة ومراجعة المطالبات، إعادة الإرسال، التعديل، التصدير.

تكاملات مستقبلية (Webhook / API) مع أنظمة مالية/أسطول/مكاتب مطالبات.

🔷 القسم 2: المتطلبات الوظيفية (Functional Requirements)

2.1 نموذج إنشاء مطالبة

حقول مطلوبة:

شركة التأمين (قائمة منسدلة ديناميكية من DB).

اسم العميل (نص قصير).

رقم الهوية / الإقامة.

رقم الحادث / رقم المطالبة / رقم وثيقة التأمين (حقول منفصلة قابلة للإخفاء حسب الشركة).

مبلغ المطالبة (قيمة رقمية + عملة SAR افتراضيًا).

تفاصيل المطالبة (نص متعدد الأسطر).

تاريخ الحادث (DatePicker).

مدينة / منطقة (اختياري).

نوع التغطية: ضد الغير / شامل / أخرى.

تحميل ملفات متعددة (drag & drop + select) بأنواع: PDF, JPG, PNG, DOCX (قابلة للتهيئة).

وسم (Tags) مثل: طبي، مركبة، ممتلكات، إصلاح، تعويض نقدي.

2.2 رفع ملفات متعددة

دعم حتى حجم X ميجابايت لكل ملف (متغير إعداد).

إظهار معاينات صور مصغّرة.

تحويل تلقائي لصور إلى PDF واحد (اختياري بإعداد).

حفظ الملفات بأسماء فريدة + ارتباط بالمطالبة.

2.3 إرسال المطالبة

عند النقر على "إرسال":

التحقق من الحقول (Validation).

توليد رسالة إيميل رسمية ديناميكية.

إدراج بيانات المطالبة آليًا في نص الإيميل حسب قالب الشركة.

إرفاق جميع الملفات.

إرسال عبر SMTP (إعدادات من ملف تهيئة / متغيرات بيئية).

تسجيل نتيجة الإرسال (نجاح/فشل + رسالة الخطأ).

2.4 نص البريد الافتراضي (يُستخدم عند عدم وجود قالب خاص بالشركة)

الموضوع: مطالبة مالية بخصوص الحادث رقم {{claim_incident_number}}

نص عربي:

السادة شركة {{company_name}} المحترمين،

أرفق لكم بيانات المطالبة الخاصة بالحادث رقم {{claim_incident_number}}:

اسم العميل: {{client_name}}
رقم الهوية: {{client_national_id}}
رقم الوثيقة: {{policy_number|اختياري}}
مبلغ المطالبة: {{claim_amount}} ريال سعودي
تاريخ الحادث: {{incident_date}}
نوع التغطية: {{coverage_type}}

تفاصيل المطالبة:
{{claim_details}}

مرفق مع هذا الإيميل المستندات المطلوبة.

شاكرين تعاونكم.

نص إنجليزي (Fallback):

Dear {{company_name}} Claims Team,

Please find attached the claim documents related to Incident No. {{claim_incident_number}}.

Insured Name: {{client_name}}
National ID: {{client_national_id}}
Policy No.: {{policy_number}}
Claim Amount: {{claim_amount}} SAR
Incident Date: {{incident_date}}
Coverage Type: {{coverage_type}}

Claim Details:
{{claim_details}}

All supporting documents are attached.

Thank you.

مهم: القوالب قابلة للتخصيص لكل شركة في DB.

🔷 القسم 3: دليل شركات التأمين في السعودية (Seed Data)

أنشئ جدول insurance_companies يحتوي على:

id (int, pk)

name_ar (نص عربي)

name_en

claims_email_primary

claims_email_cc (اختياري متعدد; تخزين JSON أو جدول فرعي)

policy_portal_url (اختياري)

notes

active (bool)

بيانات أولية (Seed — استخدم عناوين افتراضية يمكن تعديلها لاحقًا):

name_ar

name_en

claims_email_primary

التعاونية للتأمين

Tawuniya

claims@tawuniya.com.sa

ميدغلف

Medgulf

claims@medgulf.com.sa

بوبا العربية

Bupa Arabia

claims@bupa.com.sa

ولاء للتأمين

Walaa

claims@walaa.com

أسيج

ACIG

claims@acig.com.sa

سلامة

Salama

claims@salama.com

الخليجية العامة (GIG)

GIG Saudi

claims@gig.com.sa

ملاذ للتأمين

Malath

claims@malath.com.sa

الدرع العربي

Arabian Shield

claims@arabianshield.com

سايكو

SAICO

claims@saico.com.sa

الاتحاد للتأمين

Al-Etihad

claims@alethiad.com.sa

أمانة

Amana

claims@amana.com.sa

المتحدة

TUIC

claims@tuic.com.sa

العربية للتأمين

AIC

claims@aic.com.sa

أضف بقية الشركات عند الحاجة. اجعل سكربت seed قابلاً للإضافة المتكررة (idempotent insert-or-update).

🔷 القسم 4: نموذج قاعدة البيانات (Data Model)

جداول أساسية:

claims

id (uuid أو int)

company_id (fk → insurance_companies)

client_name

client_national_id

policy_number (nullable)

incident_number (nullable)

incident_date (date)

claim_amount (decimal)

currency (افتراضي SAR)

coverage_type (enum: third_party, comprehensive, other)

claim_details (text)

status (enum: draft, ready, sent, failed, acknowledged, paid)

email_message_id (nullable)

email_sent_at (datetime)

created_at, updated_at

created_by_user_id (fk users)

claim_attachments

id

claim_id (fk)

original_filename

stored_filename

mime_type

file_size

storage_path

uploaded_at

extracted_text (nullable — بعد OCR)

doc_type (enum: najim_report, id_copy, invoice, photo, other)

users (للمصادقة)

id

full_name

email (login)

password_hash

role (admin, claims_agent, viewer)

active

email_logs

id

claim_id

to_emails

cc_emails

subject

body_preview

send_status (success/failed)

error_message

sent_at

(يمكن دمج logs مع claims.status لو أردت تبسيط.)

🔷 القسم 5: الأمان (Security)

مصادقة مستخدمين (جلسات + Flask-Login أو Django Auth).

كلمات مرور مُشفّرة (bcrypt/argon2).

رفع ملفات: تحقّق من النوع والحجم، وتوليد أسماء آمنة.

تخزين الملفات خارج wwwroot أو استخدم تخزين S3 موقّع.

CSRF حماية النماذج.

تقييد الامتدادات (PDF/JPEG/PNG فقط افتراضيًا).

إعداد حد أقصى للرفع (MAX_CONTENT_LENGTH).

ملف .env لإعدادات السرية (SMTP, DB, SECRET_KEY).

🔷 القسم 6: ميزات ذكاء اصطناعي (اختيارية لكن مطلوبة في التصميم)

أضف وحدات (Modules) يمكن تفعيلها/تعطيلها:

OCR استخراج بيانات من PDF/صور: رقم الحادث، اسم العميل، تاريخ، رقم لوحة.

تصنيف نوع المستند (ID، تقرير نجم، فاتورة، صورة ضرر).

اقتراح تعبئة الحقول تلقائيًا بناءً على المرفقات.

كشف نقص المستندات: إذا ما تم رفع تقرير نجم، نبه المستخدم.

تحليل المبالغ مقارنة بسوابق تعويض (مستقبلي).

أضف واجهة إعداد لتفعيل/تعطيل الذكاء الاصطناعي.

🔷 القسم 7: لوحة التحكم (Dashboard)

وظائف:

جدول المطالبات (قابل للبحث والتصفية حسب الشركة/التاريخ/الحالة).

رمز حالة ملون (Draft = رمادي، Sent = أزرق، Failed = أحمر، Paid = أخضر).

زر عرض (تفاصيل كاملة + مرفقات + سجل البريد).

زر إعادة إرسال (Resend) → يسمح بتغيير الشركة أو الإيميل قبل الإرسال.

زر تنزيل ملف ZIP لكل المستندات.

تصدير CSV/Excel لتقارير مالية.

🔷 القسم 8: تدفقات العمل (Workflows)

8.1 إنشاء وحفظ كمسودة

المستخدم يملأ الحقول ويرفع الملفات.

يحفظ كـ Draft دون إرسال.

8.2 إرسال فوري

يضغط إرسال → إرسال بريد → تحديث الحالة.

8.3 إعادة إرسال / تعديل

ادخل المطالبة → عدّل الحقول → إعادة إرسال.

سجل تاريخ كل إرسال.

8.4 معالجة فشل الإرسال

إذا SMTP فشل، خزن الخطأ وبيّن زر "إعادة المحاولة".

🔷 القسم 9: إعدادات النظام (Settings)

واجهة إعداد (Admin Only):

إعداد بيانات إرسال البريد (SMTP host, port, user/pass, use_ssl).

البريد الافتراضي للنسخ (CC/BCC) داخلي.

حجم الملف الأقصى.

الامتدادات المسموحة.

قوالب البريد لكل شركة (WYSIWYG أو Markdown + Jinja vars).

🔷 القسم 10: هيكلة المشروع المقترحة (Flask مثال)

project_root/
  .env.example
  requirements.txt
  wsgi.py
  run.py
  config.py
  instance/config.py (للإعدادات السرية)
  app/
    __init__.py
    extensions.py (db, login, mail, migrate)
    models.py (أو مجلد models/)
    email_utils.py
    ai_utils.py (OCR, تصنيف)
    forms/
    routes/
      claims.py
      companies.py
      auth.py
      admin.py
    templates/
      layout.html
      claims/
      admin/
    static/
    translations/ (ar, en)
  migrations/
  seed/
    seed_insurance_companies.py
  tests/
    test_claim_create.py
    test_email_send.py
    test_file_upload.py

🔷 القسم 11: التعليمات التشغيلية (Dev Setup)

اطلب من النموذج توليد:

ملف requirements.txt.

أوامر إنشاء البيئة وتشغيل السيرفر المحلي.

أمر تهيئة قاعدة البيانات (Flask-Migrate أو Django migrate).

سكربت seed لشركات التأمين.

تعليمات توليد مستخدم Admin أول.

🔷 القسم 12: الاختبارات (Tests)

اختبارات وحدة + تكامل:

رفع ملف صحيح.

منع رفع امتداد خطير.

بناء إيميل بقيم المتغيرات.

فشل SMTP → حالة Failed.

نجاح الإرسال → حالة Sent + سجل log.

🔷 القسم 13: دعم تعدد اللغات (i18n)

الواجهة عربية افتراضيًا.

دعم إنجليزي (زر تبديل لغة).

رسائل البريد ثنائية اللغة (حسب إعداد الشركة أو ثابت عربي).

🔷 القسم 14: النشر (Deployment)

اطلب ملفات:

Dockerfile + docker-compose (ويب + DB + worker).

إعداد Gunicorn + Nginx.

دعم تخزين الملفات على القرص أو S3.

جدولة نسخ احتياطية لقاعدة البيانات.

🔷 القسم 15: متغيرات البيئة (ENV Vars)

APP_ENV=dev|prod

SECRET_KEY=

DATABASE_URL=

MAIL_SERVER=

MAIL_PORT=

MAIL_USERNAME=

MAIL_PASSWORD=

MAIL_USE_SSL=true/false

MAX_UPLOAD_MB=25

🔷 القسم 16: واجهة برمجية (API) مستقبلية

صمّم طبقة Service بحيث يمكن لاحقًا إضافة REST API:

POST /api/claims

GET /api/claims/

POST /api/claims//send

GET /api/companies

(لا يجب تنفيذها كاملة الآن، لكن هيكل الكود يسمح.)

🔷 القسم 17: مثال استخدام (User Story)

موظف مطالبات يسجّل الدخول.

ينشئ مطالبة جديدة لشركة التعاونية.

يرفع تقرير نجم + هوية + صور.

يضغط إرسال.

النظام يرسل البريد + يخزن الحالة.

لاحقًا يرى أنها "مرسلة"؛ في حال رد الشركة خارجيًا، يتم تحديث الحالة يدويًا.

🔷 القسم 18: مخرجات مطلوبة من مولّد الكود

عند تنفيذ هذا البرومبت، يجب أن تُرجع:

وصف سريع للمشروع والاعتمادات.

ملف requirements.txt.

ملف/ملفات إعداد (config.py, settings.py).

نماذج قاعدة بيانات (SQLAlchemy أو Django ORM).

نماذج فورم + مسارات.

قوالب HTML (RTL للعربي + LTR للإنجليزي).

منطق رفع ملفات وتخزينها.

منطق إرسال الإيميل بالقوالب.

سكربت seed للشركات.

أوامر تشغيل محلية.

اختبارات أساسية.

ملاحظات أمن وتشغيل.

🔷 القسم 19: الإدخال النهائي (انسخه لمولّد الكود)

انسخ النص التالي بالكامل، ثم الصق في أداة الذكاء الاصطناعي التي ستولّد لك المشروع:

### الدور
أنت مهندس برمجيات كبير محترف Python (Flask أو Django) ومتمكن من بناء أنظمة إدارة مطالبات تأمين مع إرسال بريد آمن ورفع ملفات.

### المطلوب
طوّر نظام ويب إنتاجي (Production-Ready) بناءً على مواصفات "نظام المطالبات المالية لشركات التأمين في السعودية" أعلاه. اقرأ كل الأقسام (0 → 18) ونفّذ بدقة.

### المتطلبات الأساسية
- واجهة إدخال مطالبة (حقول محددة).
- رفع ملفات متعددة.
- اختيار شركة → إيميل مخصص.
- قالب بريد ديناميكي (عربي + إنجليزي).
- إرسال SMTP آمن.
- قاعدة بيانات (SQLite dev / MySQL prod).
- سجل إرسال بريد.
- لوحة تحكم للمطالبات.
- إعدادات نظام (Admin).
- Seed لشركات التأمين.
- أمن (Auth, CSRF, قيود ملفات, .env).
- دعم AI (OCR + اقتراح تعبئة) اختياري عبر وحدة معطلة افتراضيًا.

### المطلوب في الخرج
أعد مشروعًا كاملاً منظمًا بملفات ومجلدات، مع شرح تشغيل محلي، وشرح متغيرات البيئة، وملف seed، وملفات قوالب بريد. استخدم أفضل ممارسات الكود النظيف.

ابدأ بإعطائي: (1) نظرة بنية المشروع، (2) المتطلبات، ثم (3) الكود ملفًا ملفًا.

🔷 القسم 20: تعليمات للاستخدام العملي معك (Lyra/المساعد)

عندما أقول لك: "ولّد المشروع" → ابدأ بإخراج الهيكل، ثم الكود تدريجيًا.
عندما أقول: "أضف شركة جديدة" → حدّث seed + واجهة.
عندما أقول: "فعّل الذكاء الاصطناعي" → أضف وحدة OCR واستخراج.